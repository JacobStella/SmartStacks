const LibraryHeader = () => {
    return (
        <header className="library-header">
            <h1>Your Library</h1>
            {/* Add more header content here if needed */}
        </header>
    );
};